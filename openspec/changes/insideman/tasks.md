## 1. Bootstrap and validate against jg-jiahd (no pipeline changes yet)

- [x] 1.1 ~~Run `talosctl config new --roles=os:reader` from break-glass~~ **Superseded**: created a dedicated Omni service account `talos-mcp-jg-jiahd` (Omni Role: Reader, 365d) via the Web UI, then `omnictl talosconfig -c jg-jiahd` for that identity — this naturally yields an `os:reader`-ceiling talosconfig, no cert-minting needed. Minting via `talosctl config new --roles=...` is a dead end regardless of source credential: requires caller to hold `os:admin`, which no Omni-obtainable identity does (confirmed for Admin-role user, Admin-role SA, and `--break-glass`, which Omni hardcodes to operator-level). Also discovered: must route through jcom's nginx-ingress gRPC endpoint (`omni-grpc.janncot.duckdns.org`), not the Cloudflare-Tunnel-fronted `omni.janncot.com` — the Tunnel breaks gRPC trailers for the Talos siderov1 proxy specifically. Saved to `jg-jiahd/talos/clusterconfig/talos-mcp-sa-env.sh` (not committed).
- [x] 1.2 Verified against jg-jiahd nodes using the Reader SA's talosconfig (via the nginx-ingress endpoint): `talosctl version --short`, `etcd members`, `get links` all succeed. Mutating/admin-only surface confirmed rejected: `talosctl config new --roles=os:reader` (cert-minting, the most sensitive Talos RPC) → `PermissionDenied: not authorized`. Did not additionally test a live `reboot`/`service restart` to avoid any disruption risk — the admin-only rejection is sufficient evidence the role ceiling is enforced server-side.
- [x] 1.3 Write `talos_mcp_server.py` in `k8scc` (FastMCP, HTTP/SSE transport, bind `127.0.0.1:<port>`) with tools: `get_node_status`, `get_etcd_members`, `get_link_status`, `get_service_logs` — each shelling out to the bundled `talosctl` binary against `TALOSCONFIG` env/path.
- [x] 1.4 Ran `talos_mcp_server.py` in a throwaway pod in jg-jiahd's `claudecode` namespace, called all four tools over real MCP/SSE (not just raw `talosctl`) via a small Python `mcp` client script. All four returned correct live data (version, etcd members x3, full link list including kubespan/siderolink, kubelet log tail). **Required an unplanned fix along the way**: found the talosconfig alone has no key material (no local PGP key exists in a fresh container) — auth additionally needs `OMNI_SERVICE_ACCOUNT_KEY` + `OMNI_ENDPOINT` present in the process env (talosctl's own auth library reads these directly). Folded into the pipeline as two new secret fields (see 3.1/3.2/3.3/3.5). Also caught or fixed along the way: **(a)** `mcp` PyPI package 2.0.0 dropped `mcp.server.fastmcp.FastMCP` entirely (breaking API change, released after the image last built) — pinned to `mcp==1.29.0` in `k8scc/Dockerfile`, rebuilt, pushed as image tag `dc6343e`; **(b)** confirmed this closes out 2.6 below. Test pod/Secrets deleted after verification.

## 2. k8scc image changes

- [x] 2.1 Add `talosctl` binary download to the Dockerfile (pin version to match the Talos server version in use; parametrize similarly to `TTYD_VERSION`).
- [x] 2.2 Add `talos_mcp_server.py` to the image (`COPY` + chmod, alongside `memory_mcp_server.py`).
- [x] 2.3 Confirm the `talos-mcp` container will run via a `command` override (`python3 /usr/local/bin/talos_mcp_server.py`) rather than `entrypoint.sh` — no changes needed to `entrypoint.sh`/`claude-session` for the sidecar's own startup.
- [x] 2.4 Add the remote-MCP registration block to `claude-session`: when the sidecar's endpoint is present (env-gated, matching the existing `claudecode_auth0_domain`-conditional pattern), register it in `~/.claude/settings.json` as a remote MCP server (URL-based), alongside the existing local `memory` stdio entry.
- [x] 2.5 Update `k8scc/CLAUDE.md`: document the `talos-mcp` sidecar, its credential model, and the tool surface. While in there, fix the pre-existing staleness noted during design research (dead `AUTH0_DOMAIN`/`AUTH0_CLIENT_ID` device-flow entries in the Runtime Configuration table; missing `TTYD_INTERFACE`/`TTYD_AUTH_HEADER`).
- [x] 2.6 Built and pushed via CI (GitHub Actions, `Build Claude Code Image` workflow). Current tag: `dc6343e` (supersedes `c50034e` — see 1.4's `mcp==1.29.0` pin fix).

## 3. jg-cluster-template pipeline changes

- [x] 3.1 Added `talos_mcp_config?`, plus (found necessary in 1.4) `talos_mcp_sa_key?` and `talos_mcp_omni_endpoint?`, all `string & !=""`, to `.taskfiles/template/resources/cluster.schema.cue`.
- [x] 3.2 Added `TALOS_MCP_CONFIG_B64` (b64, as planned) plus `TALOS_MCP_SA_KEY` and `TALOS_MCP_OMNI_ENDPOINT` (both plain single-line strings, no b64 needed) to `templates/config/kubernetes/components/sops/cluster-secrets.sops.yaml.j2`.
- [x] 3.3 Decide and implement: dedicated `talos-mcp-secret` vs. new key on existing `claude-code-secret` (see design.md Open Questions) — add the corresponding Flux-var-substituted Secret definition under `jg-base/kubernetes/apps/extras/claudecode/claude-code/app/` if dedicated, or extend `secret.yaml` if not. **Decided: dedicated** (`talos-mcp-secret.yaml`) — now carries `data.talosconfig` plus `stringData.saKey`/`stringData.omniEndpoint` (both valid on one Secret manifest simultaneously).
- [x] 3.4 Decide and implement: Secret-volume-file vs. base64-env for how `talos-mcp` consumes the credential (see design.md Open Questions). **Decided: volume-file** for the talosconfig itself (`data.talosconfig`, base64 pre-encoded, mounted at `/etc/talos-mcp/talosconfig`) **plus two plain env vars** (`OMNI_SERVICE_ACCOUNT_KEY`, `OMNI_ENDPOINT`) for the actual auth material — discovered in 1.4 that the file alone doesn't carry key material at all.
- [x] 3.5 Added the `talos-mcp` container block to `templates/config/kubernetes/apps/extras/claudecode/claude-code/instances/helmrelease.yaml.j2` (mirrors the existing `oauth2-proxy` container block: own image/command, own securityContext non-root + `drop: ALL`, own resources sized like `oauth2-proxy`, own volume/env for the credential, no shared mounts with `app`). Gated on `talos_mcp_config is defined`, independent of the auth0/OIDC gate. Env now includes `TALOSCONFIG` (volume path) + `OMNI_SERVICE_ACCOUNT_KEY`/`OMNI_ENDPOINT` (`secretKeyRef` to the two new `talos-mcp-secret` keys).
- [x] 3.6 Document `talos_mcp_config` in `cluster.sample.yaml` under the `claudecode/claude-code` extra section, including the bootstrap command from task 1.1 as inline guidance for future client onboarding. **Superseded by 1.1's actual finding** — doc needs updating (see task 3.7) to describe the real bootstrap: create an Omni service account with Omni Role Reader, then `omnictl talosconfig -c <cluster>` for that identity, routed through a non-Cloudflare-Tunnel gRPC endpoint. The old "needs os:admin to mint" framing no longer applies since nothing is minted.
- [x] 3.7 Rewrote `cluster.sample.yaml`'s guidance block: three-step bootstrap (create Reader-role Omni SA → `omnictl talosconfig` for that identity → point `talos_mcp_omni_endpoint` at a direct, non-Cloudflare-Tunnel gRPC Ingress), three fields to fill in (`talos_mcp_config`, `talos_mcp_sa_key`, `talos_mcp_omni_endpoint`).

## 4. Roll out to jg-jiahd and verify end-to-end

- [ ] 4.1 Mirror the `instances/helmrelease.yaml.j2` sidecar block into jg-jiahd's rendered `kubernetes/apps/extras/claudecode/claude-code/instances/helmrelease.yaml` (same two-repo sync convention used for all prior `claude_instances` changes this session).
- [ ] 4.2 Add jg-jiahd's `talos_mcp_config` (base64 of the task-1.1 credential) to jg-jiahd's `cluster.yaml`, run `task configure`.
- [ ] 4.3 Commit and push jg-cluster-template, jg-base (if secret definition added there), and jg-jiahd changes; reconcile Flux.
- [ ] 4.4 Verify the `talos-mcp` container comes up healthy in the `cc` pod, and confirm (e.g. via `kubectl exec` into `app`) that the credential file/env is genuinely absent from the `app` container's filesystem.
- [ ] 4.5 From a real ttyd session (as the Claude Code agent, not via kubectl), call each of the four MCP tools and confirm correct results against jg-jiahd's live nodes.
- [ ] 4.6 Confirm a mutating attempt through the MCP path is rejected (either by omission — no such tool exists — and/or by directly testing the underlying credential's server-side rejection as in task 1.2).
