---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: synophoto
  namespace: linebot
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: synophoto
  template:
    metadata:
      labels:
        app: synophoto
    spec:
      containers:
        - name: app
          image: ghcr.io/ferry133/synology-photo-tagger:latest@sha256:450e8415fc39b3dff9e3f1c57e6dbd36a12cd26f9d3564cf9970d4f0436c9c2e
          imagePullPolicy: Always
          ports:
            - containerPort: 5001
          envFrom:
            - secretRef:
                name: synophoto-secret
          env:
            - name: LINEBOT_BASE_URL
              value: http://linebot-admin.linebot.svc.cluster.local:8081
            # Match linebot-admin's code defaults (it doesn't read these from a
            # secret either). If real creds get deployed via linebot-secret in
            # the future, change both sides at once.
            - name: LINEBOT_ADMIN_USER
              value: admin
            - name: LINEBOT_ADMIN_PASS
              value: changeme
            - name: PROGRESS_ALBUM_MODEL
              value: claude-haiku-4-5
          volumeMounts:
            - name: sessions
              mountPath: /root/.syno_sessions
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          readinessProbe:
            tcpSocket:
              port: 5001
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: sessions
          persistentVolumeClaim:
            claimName: synophoto-sessions
---
apiVersion: v1
kind: Service
metadata:
  name: synophoto
  namespace: linebot
spec:
  selector:
    app: synophoto
  ports:
    - port: 5001
      targetPort: 5001
