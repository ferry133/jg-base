---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ${DB_STORAGE_CLASS:=sc-nas}
  resources:
    requests:
      storage: 5Gi
