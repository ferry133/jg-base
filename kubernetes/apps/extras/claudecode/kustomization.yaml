---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# claude-code itself lives in apps/base/claudecode — it is installed on every
# cluster, and it also owns the claudecode namespace. Only the optional
# dedicated database stays opt-in here.
resources:
  - ./postgres/ks.yaml
