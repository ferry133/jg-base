---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: offsite-backup
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/apps/base/monitoring/backup/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: jg-base
    namespace: flux-system
  targetNamespace: monitoring
  wait: false
